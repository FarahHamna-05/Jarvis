{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd supplyguard-frontend && npm install && npm run build",
  "outputDirectory": "supplyguard-frontend/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
